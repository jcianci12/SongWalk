services:
  songshare:
    container_name: songwalk
    restart: unless-stopped
    environment:
      SONGSHARE_BASE_URL: https://songwalk.tekonline.com.au
      SONGSHARE_QUICK_TUNNEL_ENABLED: "0"
      SONGSHARE_PROXY_HOPS: "1"
    volumes:
      - songwalk-data:/data

volumes:
  songwalk-data:
