services:
  songshare:
    build: .
    ports:
      - "${SONGSHARE_PUBLISHED_PORT:-8080}:8080"
    environment:
      SONGSHARE_DATA_DIR: /data
      SONGSHARE_BASE_URL: ${SONGSHARE_BASE_URL:-}
      SONGSHARE_PROXY_HOPS: ${SONGSHARE_PROXY_HOPS:-1}
    volumes:
      - ./songshare-data:/data
